const a = 10;
a = 20;
{
    a = "madhav"
    console.log(a);
}
console.log(a);

// in const we will know that how constant value works if we giv a value to the constant hthen we will not change the value of that contant variable.
// if we can give a const as a variable the we will not change the vallue and we will not reinitialize the values.